import machine
import utime

led = machine.Pin(25, machine.Pin.OUT)

while True:
    led.toggle()
# here is where you can change the value to turn the led on or off with led.value(1) for on and led.value(0) for off    
    
    
 
